import { TextField } from "@mui/material";
import styled from "styled-components";

export const SelectCurrency = styled(TextField)`
  &.MuiTextField-root {
    margin-bottom: 10px;
  }
`;

export const TextFieldCurrency = styled(TextField)`
  &.MuiTextField-root {
    margin-bottom: 50px;
  }
`;
