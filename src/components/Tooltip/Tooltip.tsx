import * as Styles from "./Tooltip.styles";

const Tooltip = () => {
  return (
    <Styles.Tooltip>
      *TIP: You can change the currency by clicking on the flag
    </Styles.Tooltip>
  );
};

export default Tooltip;
